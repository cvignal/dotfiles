#!/bin/sh

xrandr | grep -q "HDMI-0 connected" && #!/bin/sh
xrandr --output DP-0 --off --output DP-1 --off --output DP-2 --off --output HDMI-0 --primary --mode 2560x1440 --pos 0x0 --rotate normal --output DP-3 --off --output DP-4 --off
